# SPEC_CHARACTER.md - 플레이어 능력치 시스템

> 자세한 수치는 총정리.md의 "캐릭터 능력치 시스템" 참조

---

## 📊 능력치 구조

```
public class PlayerStats {
    // 기본 능력치
    int maxHealth = 100;
    int currentHealth = 100;
    int maxMana = 50;
    int currentMana = 50;

    // 레벨 진행에 따른 능력치
    int attack = 15;
    int defense = 3;
    float speed = 300f;  // 픽셀/초

    // 레벨 정보
    int level = 1;
    int experience = 0;
    int maxExperienceForLevel = 50;
}
```

---

## 📈 레벨업 보상

**레벨 1 기준점 (초기값)**:
```
HP: 100
MP: 50
ATK: 15
DEF: 3
SPD: 300 px/s
```

**레벨업 시 증가량** (각 레벨마다):
```
Lv 1→2: HP +8, MP +10, ATK ×1.1, DEF +2, SPD ×1.05
Lv 2→3: HP +8, MP +10, ATK ×1.1, DEF +2, SPD ×1.05
...
Lv 14→15: HP +8, MP +10, ATK ×1.1, DEF +2, SPD ×1.05
```

**최대 레벨(15) 도달 시**:
```
MaxHP = 100 + 8 × 14 = 212
MaxMP = 50 + 10 × 14 = 190
ATK = 15 × 1.1^14 ≈ 53
DEF = 3 + 2 × 14 = 31
SPD = 300 × 1.05^14 ≈ 655
```

---

## ❤️ 체력 관리

### 자동 회복
```
회복 주기: 10초마다 한 번
체력 회복량: maxHealth * 0.03 (최대 체력의 3%)

예시:
  현재 HP: 80 / 100
  회복 후: 80 + (100 * 0.03) = 80 + 3 = 83
```

### 스킬 피해 시 생명력 흡수 (추후 구현)
```
(미정 - 게임 밸런싱 후 결정)
일반적으로: 입힌 데미지의 10~20% 회복
```

---

## 💙 마나 관리

### 자동 회복
```
회복 주기: 10초마다 한 번
마나 회복량: maxMana * 0.05 (최대 마나의 5%)

예시:
  현재 MP: 20 / 50
  회복 후: 20 + (50 * 0.05) = 20 + 2.5 = 22.5
```

### 스킬 시전 시 소비
```
각 스킬마다 다른 마나 비용:
  매직 미사일: 0 (소비 없음)
  불 스킬 A: 30
  불 스킬 B: 25
  불 스킬 C: 50
  ...
```

### 마나 부족 처리
```
현재_마나 < 필요_마나
  → 스킬 시전 불가
  → 클라이언트에서 버튼 회색 처리
```

---

## 💥 공격력 계산

### 공격력 공식
```
플레이어_ATK = 기본_ATK × (1.1 ^ (레벨 - 1))

예시:
  레벨 1: ATK = 15 × 1.1^0 = 15
  레벨 2: ATK = 15 × 1.1^1 = 16.5
  레벨 3: ATK = 15 × 1.1^2 = 18.15
  레벨 5: ATK = 15 × 1.1^4 ≈ 21.95
```

### 스킬 데미지
```
스킬_데미지 = 스킬_기본_데미지 + 플레이어_ATK

예시:
  현재 ATK: 20
  불 스킬 A (기본 40 데미지):
    최종_데미지 = 40 + 20 = 60
```

---

## 🛡️ 방어력 계산

### 방어력 효과
```
데미지_감소 = 방어력 × 2

예시:
  플레이어 DEF: 5
  받은_데미지: 30
  감소량: 5 × 2 = 10
  최종_데미지: 30 - 10 = 20

  (만약 방어력이 높으면)
  플레이어 DEF: 20
  받은_데미지: 30
  감소량: 20 × 2 = 40 (40은 30보다 크므로)
  최종_데미지: max(1, 30 - 40) = 1 (최소 1)
```

---

## 🏃 이동속도

### 기본 속도
```
초기 SPD: 300 픽셀/초
레벨당 증가: +5% (×1.05)

레벨별 속도:
  Lv 1:  300 px/s
  Lv 5:  300 × 1.05^4 ≈ 365 px/s
  Lv 10: 300 × 1.05^9 ≈ 465 px/s
  Lv 15: 300 × 1.05^14 ≈ 655 px/s
```

### 이동 계산
```
위치_업데이트 = 현재_위치 + (조이스틱_방향 × SPD × delta)

예시 (delta = 1/60초 = 0.0167초):
  조이스틱: (1, 0) (오른쪽)
  SPD: 300
  이동거리: 300 × 0.0167 = 5 픽셀

  = 60fps 기준 초당 약 300픽셀 이동
```

---

## 🔄 상태 관리

### PlayerState Enum
```
IDLE         (기본, 움직이지 않음)
MOVING       (이동 중)
CASTING      (스킬 시전 중)
DAMAGED      (피해 입는 중, 애니메이션)
DEAD         (사망)
```

---

## 📋 PlayerStats 클래스

```
public class PlayerStats {
    // 초기화 (레벨 1)
    public PlayerStats() {
        maxHealth = 100;
        currentHealth = 100;
        maxMana = 50;
        currentMana = 50;
        attack = 15;
        defense = 3;
        speed = 300f;
        level = 1;
        experience = 0;
    }

    // 경험치 획득
    public void gainExperience(int exp) {
        experience += exp;
        if (experience >= requiredExpForLevel) {
            levelUp();
        }
    }

    // 레벨업
    public void levelUp() {
        level++;
        maxHealth += 8;
        currentHealth = maxHealth;  // 회복
        maxMana += 10;
        currentMana = maxMana;  // 회복
        attack *= 1.1f;
        defense += 2;
        speed *= 1.05f;
        experience = 0;  // 경험치 초기화
        // calculateRequired(); // 다음 레벨 필요 경험치 계산
    }

    // 피해 입기
    public void takeDamage(int damage) {
        int actualDamage = Math.max(1, damage - (defense * 2));
        currentHealth -= actualDamage;
        if (currentHealth <= 0) {
            currentHealth = 0;
            state = PlayerState.DEAD;
        }
    }

    // 자동 회복
    public void autoRecover(float delta) {
        recoverTimer += delta;
        if (recoverTimer >= 10f) {
            currentHealth = Math.min(maxHealth,
                (int)(currentHealth + maxHealth * 0.03f));
            currentMana = Math.min(maxMana,
                (int)(currentMana + maxMana * 0.05f));
            recoverTimer = 0;
        }
    }

    // 마나 소비
    public boolean consumeMana(int amount) {
        if (currentMana >= amount) {
            currentMana -= amount;
            return true;
        }
        return false;  // 마나 부족
    }
}
```

---

**경험치 테이블**: 총정리.md의 "레벨 & 경험치 시스템" 참조

